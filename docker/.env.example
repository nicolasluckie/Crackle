# Example environment variables for Docker deployment
# Copy this file to .env and customize as needed

# Flask Configuration
FLASK_ENV=production
FLASK_APP=api.py
PYTHONUNBUFFERED=1

# CORS Configuration
# In production with nginx proxy, frontend and API are same-origin
# Leave empty for same-origin setup (recommended for Docker deployment)
# Example for development or separate domains:
# ALLOWED_ORIGINS=http://localhost:5173,https://example.domain.com

# Gunicorn Configuration (uncomment to override defaults)
# WORKERS=4
# TIMEOUT=60
# BIND_ADDRESS=0.0.0.0:5000

# Application Configuration
# (Add any custom application settings here)
